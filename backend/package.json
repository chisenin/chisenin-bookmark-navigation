{
  "name": "bookmark-api",
  "version": "1.0.0",
  "description": "Bookmark API for Cloudflare Workers",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "echo 'No build steps needed'"
  },
  "dependencies": {
    "itty-router": "^3.0.12",
    "jsonwebtoken": "^9.0.1"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
